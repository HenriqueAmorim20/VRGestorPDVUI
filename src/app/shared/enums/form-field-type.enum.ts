export enum FormFieldType {
  INPUT = 'input',
  SELECT = 'select',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
  TEXT_AREA = 'text-area',
  DATE = 'date',
  TIME = 'time',
  DATE_RANGE = 'date-range',
  DATE_TIME = 'date-time',
}
