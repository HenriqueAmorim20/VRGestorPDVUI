import { DispatcherPipe } from './dispatcher.pipe';

describe('DispatcherPipe', () => {
  it('create an instance', () => {
    const pipe = new DispatcherPipe();
    expect(pipe).toBeTruthy();
  });
});
